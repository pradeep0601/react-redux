import React from 'react';

const cakeContainer = () => {
    return (
        <div>
            <h2>No of cakes</h2>
            <button>Buy Cake</button>
        </div>
    );
};

export default cakeContainer;